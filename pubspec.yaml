name: tam_app_v2
description: "TAM app v2 Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  font_awesome_flutter: ^10.7.0
  material_symbols_icons: ^4.2719.3

  http: ^1.2.1
  url_launcher: ^6.2.6

  bloc: ^8.1.4
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5

  firebase_core: ^2.30.1
  firebase_auth: ^4.19.4

  user_repository:
    path: packages/user_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/logo/
